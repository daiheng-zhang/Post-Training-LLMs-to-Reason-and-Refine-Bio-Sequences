name: stride-public
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch>=2.2
  - cudatoolkit
  - rdkit
  - pyarrow
  - pandas
  - pip:
      - -r requirements/train.txt
